language: go
go:
 - 1.8
install: make

after_success: sed -i "s/NEWRELICLICENSEKEY/$NEWRELIC_LICENSE_KEY/g" ./manifest.yml

deploy:
   edge: true
   provider: cloudfoundry
   username: $BM_USERNAME
   password: $BM_PASSWORD
   api: https://$BM_API
   organization: $BM_ORG
   space: dev

deploy:
   edge: true
   provider: cloudfoundry
   username: $BM_USERNAME
   password: $BM_PASSWORD
   api: https://$BM_API
   organization: $BM_ORG
   space: prod

notifications:
  slack:
    rooms:
      - tawerin:6a1af05ead7bf3f03d582497975cfb7b446fafab
    template:
    - "Build <%{build_url}|#%{build_number}> (<%{compare_url}|%{commit}>) of %{repository}@%{branch} in PR <%{pull_request_url}|#%{pull_request_number}> by %{author} %{result} in %{duration}"
